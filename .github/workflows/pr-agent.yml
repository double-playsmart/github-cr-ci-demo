name: PR Agent

on:
  pull_request:
    types: [opened, reopened, ready_for_review] #synchronize 是 PR 更新时触发
  issue_comment:           # 支持在 PR 评论里输入 /review /describe /ask 等命令

jobs:
  pr_agent:
    if: ${{ github.event.sender.type != 'Bot' && !contains(github.event.pull_request.labels.*.name, 'skip-ai-review') }}
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
      contents: read
    steps:
      - uses: qodo-ai/pr-agent@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

          # ── Claude（当前，约 ¥0.02/次）─────────────────────────
          CONFIG.MODEL: "anthropic/claude-haiku-4-5"
          # CONFIG.MODEL: "anthropic/claude-sonnet-4-6"    # ¥0.2/次，质量更好
          # CONFIG.MODEL: "anthropic/claude-opus-4-6"      # ¥1/次，最强
          ANTHROPIC.KEY: ${{ secrets.ANTHROPIC_API_KEY }}

          # ── Gemini 免费（各 20 RPD/天，额度独立可轮换）──────────
          # CONFIG.MODEL: "gemini/gemini-3-flash-preview"
          # CONFIG.MODEL: "gemini/gemini-2.5-flash-lite"
          # CONFIG.MODEL: "gemini/gemini-2.5-flash"
          # GOOGLE_AI_STUDIO.GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

          # ── GPT（付费）─────────────────────────────────────────
          # CONFIG.MODEL: "openai/gpt-4o"
          # OPENAI.KEY: ${{ secrets.OPENAI_API_KEY }}
