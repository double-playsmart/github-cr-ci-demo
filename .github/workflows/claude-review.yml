name: AI Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  gemini-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Review with Gemini
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          BASE_REF: ${{ github.base_ref }}
        run: |
          git diff origin/${BASE_REF}...HEAD > diff.txt
          python3 .github/scripts/gemini_review.py

# Claude 方案（团队目标，切换步骤见 docs/指南.md）
# 1. GitHub Secrets 加 ANTHROPIC_API_KEY
# 2. 注释掉上方 gemini-review job，取消注释下方
#
# claude-review:
#   runs-on: ubuntu-latest
#   permissions:
#     contents: read
#     pull-requests: write
#     issues: write
#     id-token: write
#   steps:
#     - name: Run Claude PR review
#       uses: anthropics/claude-code-action@v1
#       with:
#         anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
#         prompt: |
#           你是资深前端工程师，对 PR 按五维度各评 0-10 分
#           （功能正确性/代码质量/性能/安全性/可维护性），
#           输出 TOTAL: XX/50，给出中文综合建议。
