# 项目指南

> 本文是这个仓库的中文主文档，覆盖目录结构、CI/CR 原理、AI Review 配置和审查流程。
> 适合人类和 AI 协作者快速上手。

---

## 项目定位

这是一个**演示仓库**，不是业务项目。前端代码故意保持最小，重点在于把 GitHub 的协作流程跑通：

```
本地改代码 → push → 触发 CI → 提 PR → AI 自动 Review → 人工 Review → merge
```

---

## 目录结构

```
github-cr-ci-demo/
├── src/                        # 最小前端代码（Vite + 原生 JS）
│   ├── main.js                 # 页面内容，模板字符串注入 demo 卡片
│   └── style.css               # 样式，CSS Grid + 渐变
├── index.html                  # 入口，加载 src/main.js
│
├── .github/
│   ├── workflows/
│   │   ├── ci.yml              # ★ CI：push/PR 时自动跑 lint + build
│   │   └── pr-agent.yml        # ★ AI Review：PR 时自动运行 PR-Agent
│   ├── pull_request_template.md # ★ PR 模板：交付 Checklist
│   └── CODEOWNERS              # 代码负责人（需替换为真实 GitHub 用户名）
│
├── docs/
│   ├── 指南.md                 # ← 你在看的这个文件，中文主文档
│   ├── ai-review-guide.md      # AI Review 方案文档（方案/对比/迁移/FAQ）
│   └── pr-agent-config.md      # .pr_agent.toml 配置参考
│
├── .pr_agent.toml              # ★ PR-Agent 配置：审查规则、模型、中文输出
├── CLAUDE.md                   # Claude Code 工作指引
├── package.json                # npm scripts + 依赖
└── eslint.config.js            # ESLint v9 flat config
```

---

## CI 原理与配置

### 什么是 CI（Continuous Integration）

每次有人 push 代码或提 PR，GitHub 自动运行一套检查脚本，防止"坏代码"进入主分支。

### 触发条件（`.github/workflows/ci.yml`）

```yaml
on:
  push:
    branches:
      - main
      - master
      - "feature/**"   # feature/ 开头的分支 push 时触发
  pull_request:        # 所有 PR 都触发
```

**触发逻辑**：只要满足上面任意一个条件，GitHub Actions 就会自动启动。

### 执行步骤

```
1. 拉取代码（checkout）
2. 安装 Node.js 20
3. npm ci（干净安装依赖）
4. npm run lint（ESLint 检查）
5. npm run build（Vite 构建）
```

任意一步失败 → CI 标红 → PR 无法合并（需配合 branch protection 设置）。

### 本地验证（推送前必跑）

```bash
npm run check   # = lint + build，等价于 CI 跑的内容
```

---

## CR 原理与配置

### 什么是 CR（Code Review）

开发者提 PR 后，其他人（或 AI）对代码变更进行审查，发现问题、讨论方案，确认无误后批准合并。

### GitHub 上的 CR 流程

```
1. 从 main 切出 feature 分支
2. 本地改代码，commit，push
3. 在 GitHub 上 New Pull Request（选 base: main）
4. 填写 PR 模板（改了什么、为什么、风险点）
5. Reviewer 查看 diff，留 comment 或 Approve
6. CI 和 AI Review 自动运行
7. 所有检查通过 + 至少 1 人 Approve → 可以 merge
```

### Branch Protection 配置（GitHub 仓库 Settings）

```
Settings → Branches → Add branch protection rule
Branch name pattern: main

勾选：
✅ Require a pull request before merging
  ✅ Require approvals（建议 1）
✅ Require status checks to pass before merging
  → 搜索并添加 "validate"（CI job 名）
✅ Require conversation resolution before merging
✅ Do not allow bypassing the above settings
```

设置完成后，直接 push 到 main 会被拒绝，必须走 PR 流程。

---

## AI Review 配置

本项目使用 **PR-Agent**（开源）作为 AI Review 方案，支持随时切换 AI 后端。

| 方案 | 内联行评论 | 自动描述 | 交互命令 | 自定义规则 | 费用 |
|------|-----------|---------|---------|-----------|------|
| **PR-Agent（当前）** | ✅ | ✅ | ✅ `/review` `/ask` | ✅ `.pr_agent.toml` | 免费（用 Gemini 额度） |
| Gemini Code Assist（已停用） | ✅ | ❌ | ✅ `/gemini` | ✅ styleguide.md | 免费但数据安全存疑 |

---

### PR-Agent 配置（当前方案）

**相关文件**：

| 文件 | 作用 |
|------|------|
| `.github/workflows/pr-agent.yml` | 触发入口，PR 打开/更新时自动运行 |
| `.pr_agent.toml` | 审查规则、模型配置、输出语言 |

**PR 打开后自动输出两部分内容**：

**① 自动生成 PR 描述**（写入 PR 描述框）：

| 区块 | 说明 |
|------|------|
| PR Type | 改动类型（Bug / Enhancement / Other 等） |
| Description | 改动说明列表 |
| File Walkthrough | 文件变更表格 |

**② 自动发 Review 评论**（PR 评论区）：

| 区块 | 说明 | 是否启用 |
|------|------|---------|
| Estimated effort to review | 评估 review 所需精力（1-5，1=5分钟，5=1小时+） | ✅ |
| Security concerns | 安全问题分析（XSS / 敏感信息等） | ✅ |
| Recommended focus areas | 重点审查项，链接到具体代码行 | ✅ 始终显示 |
| No relevant tests | 测试覆盖提示 | ❌ 已关闭 |

**③ 可用命令**（在 PR 评论里输入，触发后约 30 秒出结果）：

| 命令 | 功能 | 演示建议 |
|------|------|---------|
| `/review` | 手动触发 review | 改代码后重新触发 |
| `/describe` | 重新生成 PR 描述 | 描述不准时用 |
| `/improve` | 给出最多 4 条具体代码改进建议（含可 commit 的 diff） | 演示 AI 修复能力 |
| `/ask 问题` | 针对这个 PR 问 AI 任何问题 | 现场问"XSS 怎么修" |
| `/add_docs` | 自动生成函数文档注释 | 可选 |
| `/test` | 自动生成测试代码 | 有测试框架时用 |
| `/update_changelog` | 自动更新 CHANGELOG 文件 | 可选 |

**④ 定制审查规则**：编辑 `.pr_agent.toml` 中的 `extra_instructions`，push 后即生效，无需重新配置。

---

### 切换 AI 后端

只需修改 `.github/workflows/pr-agent.yml` 中的两行环境变量：

**切换到 Claude（Anthropic）**：
1. GitHub Secrets 加 `ANTHROPIC_API_KEY`（在 console.anthropic.com 获取，按 token 计费，约 $0.014/PR）
2. 编辑 `pr-agent.yml`，注释掉 Gemini 的两行，取消注释 Claude 的两行

**切换到 GPT（OpenAI）**：
1. GitHub Secrets 加 `OPENAI_API_KEY`
2. 编辑 `pr-agent.yml`，切换到 GPT 那两行

三种后端的 review 质量对比：Claude ≈ GPT-4o > Gemini（但 Gemini 免费）。

---

### Gemini API 免费额度参考

| 模型 | RPM | RPD（每天请求上限） |
|------|-----|-------------------|
| gemini-2.5-flash | 10 | 20 |
| gemini-2.5-flash-lite（当前） | 15 | **1000**（高频场景推荐） |
| gemini-2.5-pro | 5 | 100 |

文档：[ai.google.dev/gemini-api/docs/rate-limits](https://ai.google.dev/gemini-api/docs/rate-limits)

---

## PR Review 完整流程

PR-Agent 采用**定性 Review**，不打数字分。业界主流工具（GitHub Copilot Review、CodeRabbit）均如此设计——定性更直接，"必须修改：XSS 漏洞" 比 "安全性 3/10" 更有行动指引。

### 一个 PR 的完整流程

```
1. 开发者提 PR，填写 PR 模板（改了什么、风险点）
2. CI 自动运行 → 约 1 分钟出结果（lint + build）
3. PR-Agent 自动运行 → 约 30 秒出结果：
   ├── 自动生成 PR 描述（类型 + 改动说明 + 文件列表）
   └── Review 评论（精力评估 + 安全分析 + 重点审查项）
4. Reviewer 打开 PR，查看 diff 和 AI 评论
5. 可在评论里用 /ask 追问，/improve 获取修复代码
6. 如有问题，在具体代码行留 comment
7. 开发者修改后 push，评论里输入 /review 重新触发
8. Reviewer Approve → Merge
```

### AI Review 输出说明

| 输出项 | 含义 | 行动建议 |
|--------|------|---------|
| Estimated effort: 1-2 | 改动小，快速过 | 5-15 分钟内 review |
| Estimated effort: 3-5 | 改动复杂 | 安排专门时间 |
| Security concerns 有内容 | 存在安全风险 | 必须处理后才能合并 |
| Recommended focus areas | 重点关注的代码位置 | 点击链接直达代码行 |

---

## 常用命令

```bash
npm run dev      # 启动开发服务器 http://localhost:5173
npm run lint     # ESLint 检查
npm run build    # Vite 生产构建
npm run check    # lint + build（推送前验证）
```

---

详细迁移步骤和踩坑记录见 [migration-guide.md](migration-guide.md)。
